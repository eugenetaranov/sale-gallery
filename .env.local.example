# Airtable read-only Personal Access Token (scope: data.records:read on base "Sale").
# Create one at https://airtable.com/create/tokens then copy the value here.
AIRTABLE_TOKEN=pat_xxxxxxxxxxxxxxxxxx

# The Sale base + Items table (already correct — no need to change).
AIRTABLE_BASE_ID=appVRg8oE1i6lzT8X
AIRTABLE_TABLE_ID=tblx10ns0xp5ImkWy
